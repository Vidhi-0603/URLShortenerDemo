# URL Shortener (Full Stack - Dockerized)

A fully containerized **URL Shortener** web application built with a **Node.js + MongoDB backend** and a **modern Vite-based frontend (React)**.\
The app lets users shorten long URLs, provide a custom alias, redirect to original links, and manage them through an elegant UI --- all with a simple Docker setup.

* * * * *

## Features
-----------

-   ✂️ **Shorten Long URLs** instantly

-   🔁 **Redirects** to the original link using unique short codes

-   🧾 **MongoDB Integration** for persistent storage

-   ⚙️ **Environment-based Configuration**

-   🐳 **Dockerized Deployment** --- one command to run all services

-   🔒 **JWT-based Authentication** (ready for extension)

-   🌐 **Frontend--Backend Communication** through REST APIs

* * * * *

🛠️ Tech Stack
--------------

| Layer | Technology |
| --- | --- |
| **Frontend** | Vite + Angular / React |
| **Backend** | Node.js + Express |
| **Database** | MongoDB |
| **Containerization** | Docker & Docker Compose |
| **Environment Handling** | `.env` file |

* * * * *

📁 Project Structure
--------------------

.
├── docker-compose.yml\
├── .env.example\
├── frontend/     # Vite-based UI\
├── backend/      # Express + MongoDB API\
└── README.md

* * * * *

⚙️ Environment Variables
------------------------

Rename `.env.example` → `.env` and adjust values if needed:
| Variable | Value |
| --- | --- |
| VITE_API_URL | http://localhost:5000/ |
| MONGODB_URI | mongodb://mongo:27017/urlShortener |
| JWT_SECRET | your_jwt_secret |


>
> -   `VITE_API_URL` tells the frontend where to reach the backend API.
>
>
> -   `MONGODB_URI` uses the internal Docker service name `mongo`.
>
>
> -   `JWT_SECRET` is required for signing authentication tokens (if you add login features).

* * * * *

🐳 Docker Setup
---------------

### 1️⃣ Clone the Repository

`git clone https://github.com/Vidhi-0603/URLShortenerDemo.git
cd URLShortenerDemo`

### 2️⃣ Create `.env` File

`cp .env.example .env`

### 3️⃣ Run All Services

`docker-compose up --build`

### 4️⃣ Access the App

-   **Frontend (UI):** <http://localhost:3000>

-   **Backend API:** <http://localhost:5000>

-   **MongoDB:** accessible internally as `mongodb://mongo:27017`

* * * * *

📡 API Endpoints
----------------

### Auth

- `POST /api/auth/register`  

  Register a new user.  

  **Body:** `{ name, email, password }`

- `POST /api/auth/login`  

  Login with email and password.  

  **Body:** `{ email, password }`

- `POST /api/auth/logout`  

  Logout the current user.

- `GET /api/auth/me`  

  Get the current authenticated user's info.

---

- - `GET /dashboard`  

  Get the current authenticated user's dashboard( urls generated by user ).

---

### URL Shortening

- `POST /api/create`  

  Create a new short URL.  

  **Body:** `{ url, customUrl? }`  

  - If authenticated, can provide `customUrl`.

- `GET /:id`  

  Redirect to the original URL for the given short URL ID.

---

### User

- `POST /api/user/urls`  

  Get all URLs created by the authenticated user.

---


**Example Request**

`POST /api/create
Content-Type: application/json

{
  "fullUrl": "https://example.com/very/long/link"
}`

**Response**

`{
  "shortUrl": "http://localhost:3000/XyZ123"
}`

* * * * *

🧰 Useful Commands
------------------

### Stop all services

`docker-compose down`

### View logs

`docker-compose logs -f`

### Rebuild containers

`docker-compose up --build -d`

* * * * *

## Author

[Vidi-0603](https://github.com/Vidhi-0603)

---
